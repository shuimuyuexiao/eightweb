NEXT_PUBLIC_SITE_URL=https://www.creativebrandinteractive.com
# Add the new company's registered office when confirmed.
NEXT_PUBLIC_REGISTERED_OFFICE=

# Optional: a form service endpoint that accepts JSON POST requests.
# Without this value, the contact form opens the visitor's email application.
NEXT_PUBLIC_CONTACT_ENDPOINT=
